{# Absolute host paths this deploy installed: a 'source' app's Quadlet files, systemd
   units and config tree, or a 'simple' app's one rendered Quadlet. Read back verbatim by
   read-manifest.yml, which validates every line before anything acts on it — a flat file
   directly inside application_system_dir / application_unit_dir, or any file under this
   app's own config tree. #}
{% for path in application_installed_paths %}
{{ path }}
{% endfor %}
